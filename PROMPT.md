study specs/*
study IMPLEMENTATION_PLAN.md and implement the most important piece (ensure tests are authored) if it has not been implemented. 
after each change run the tests, then update IMPLEMENTATION_PLAN.md if tests pass, then commit the files via git via the bash tool if these tests pass.
